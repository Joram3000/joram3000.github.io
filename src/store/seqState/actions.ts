export const PatternUpdater = (Payload: number[][]) => ({
  type: "PATTERNUPDATER",
  payload: Payload,
});

// TRANSPORT UPDATER
// export const Transportupdater = (Payload: string) => ({
//   type: "TRANSPORTUPDATER",
//   payload: Payload,
// });
